# === CHROME PROFILE SETTINGS ===

# Absolute path to the parent directory where a NEW Chrome profile folder will be created.
# ⚠️ Do NOT use the path of your actual daily Chrome profile — use a dedicated folder!
# Example (Windows): D:\ChromeProfilesForBots
CHROME_DATA_DIR=

# Name of the Chrome profile folder to be created/used inside the above directory.
# You can choose any unique name like "CookieClickerBot"
CHROME_PROFILE=
